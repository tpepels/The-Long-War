[build-system]
requires = ["setuptools>=75", "Cython>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "longwar"
version = "0.1.0"
description = "Rules, print tooling, and balance analysis for The Long War card game"
requires-python = ">=3.11"
dependencies = [
  "Markdown>=3.7",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "Cython>=3.0",
  "setuptools>=75",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
markers = [
  "algorithm: algorithm-learning and solver correctness tests",
  "integration: multi-game or end-to-end integration tests",
  "legacy_rule_experiment: historical/non-standard rules experiment coverage",
]

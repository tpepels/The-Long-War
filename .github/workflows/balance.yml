name: Balance diagnostics

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * 1"
  push:
    branches: [main]
    paths:
      - "cards/**"
      - "decks/**"
      - "src/longwar/**"
      - "tools/balance_report.py"
      - "tools/simulate.py"
      - ".github/workflows/balance.yml"

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -e .
      - run: python tools/balance_report.py
      - run: python tools/simulate.py --games 2000 --seed 1701
      - uses: actions/upload-artifact@v4
        with:
          name: balance-reports
          path: artifacts/
          retention-days: 30
